# Production API URL.
# Set this only if you need to point the Vite dev proxy at another API host.
VITE_API_BASE_URL=https://api.azimut.ai

VITE_CLIENT_ID=your_client_id
VITE_CLIENT_SECRET=your_client_secret
