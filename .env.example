# Leave empty in dev — Vite proxies /v1 → http://localhost:8080
# Set to production base URL when deploying (e.g. https://api.azimut.ai)
VITE_API_BASE_URL=
VITE_CLIENT_ID=your_client_id
VITE_CLIENT_SECRET=your_client_secret
